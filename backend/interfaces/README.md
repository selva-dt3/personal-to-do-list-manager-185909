# Interfaces

This folder contains the OpenAPI specification for the backend service.

- openapi.json: API contract used by the frontend.
- Documentation is exposed at runtime at:
  - /docs (Swagger UI)
  - /openapi.json (OpenAPI JSON)
